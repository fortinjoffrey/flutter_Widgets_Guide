import 'package:flutter/material.dart';

class SliverListsHome extends StatelessWidget {
  const SliverListsHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Sliver Lists'),
      ),
      body: Center(
        child: Text('Sliver Lists'),
      ),
    );
  }
}
