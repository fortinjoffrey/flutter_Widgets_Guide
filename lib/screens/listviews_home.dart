import 'package:flutter/material.dart';

class ListViewsHome extends StatelessWidget {
  const ListViewsHome({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('List Views'),
      ),
      body: Center(
        child: Text('List Views'),
      ),
    );
  }
}
